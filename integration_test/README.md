# Integration Test Scaffold

This directory is reserved for future native integration / E2E tests.

Current repo state:
- runnable smoke coverage lives under `test/app/app_smoke_test.dart`
- `integration_test` is configured in `pubspec.yaml`
- this directory can host device-level tests without changing app code
- running tests here still requires a target device/emulator configured locally
